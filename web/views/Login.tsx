import React from 'react';
import Layout from '../components/Layout';

const Login: React.FC = () => {
  return (
    <Layout profile={false}>
      <div className="text-center">Login View</div>
    </Layout>
  );
};

export default Login;
