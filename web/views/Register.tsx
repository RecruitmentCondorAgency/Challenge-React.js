import React from 'react';
import Layout from '../components/Layout';

const Register: React.FC = () => {
  return (
    <Layout profile={false}>
      <div className="text-center">Register View</div>
    </Layout>
  );
};

export default Register;
