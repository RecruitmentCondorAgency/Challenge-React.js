import { extendTheme } from '@chakra-ui/react'

export default theme = extendTheme({
    styles: {
        global: {
            body: {
                backgroundColor: '#F0F0F2'
            }
        }
    },
    colors: {
        brand: {
            primaryBg: '#FFFFFF',
            primaryBtn: '#2A91E0',
            fontColor: '#636C7D'
        }
    }
})