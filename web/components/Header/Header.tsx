import { Box } from "@mui/material";
import { FC } from "react";

export const Header: FC = () => {
  return (
    <Box
      sx={{
        width: "100vw",
        padding: "20px 160px",
      }}
    ></Box>
  );
};
